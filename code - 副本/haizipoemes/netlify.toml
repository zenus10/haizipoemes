[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/conversations"
  to = "/contact"
  status = 301

# 404 页面处理（Astro 是 MPA，不需要 SPA fallback）
# 如果需要自定义 404 页面，在 src/pages/404.astro 创建即可
